vast
cat ~/AGENTS.md 2>/dev/null | head -30; echo ---; pgrep dockerd && echo dockerd-already-running || (nohup dockerd > /tmp/dockerd.log 2>&1 & sleep 10; docker ps >/dev/null 2>&1 && echo DOCKER-WORKS || (echo DOCKER-FAILED; tail -15 /tmp/dockerd.log))
