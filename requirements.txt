# Consolidated, pinned dependencies for all CourtSide-Edge agents.
# Per-agent requirements.txt files (used by Dockerfiles) are SUBSETS of these
# pins - keep any version they share with this file in sync.
curl_cffi==0.7.1
fastapi==0.110.0
numpy==1.26.4
pandas==2.2.1
pydantic==2.13.4
pymc==5.12.0
redis==5.0.3
requests==2.31.0
schedule==1.2.1
scikit-learn==1.4.1.post1
scipy==1.13.0
uvicorn==0.29.0
xgboost==2.0.3

# Test tooling
pytest==8.2.0
fakeredis==2.36.1
ruff==0.4.4
