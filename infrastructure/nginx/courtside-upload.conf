# JSON base64 expands the advertised 10 MB screenshot limit by roughly one third. Nginx's
# default 1 MB request limit rejected normal slips before they reached FastAPI, so leave enough
# room for the encoded image and its small metadata envelope. The application still enforces the
# exact decoded 10,000,000-byte limit.
client_max_body_size 14m;
